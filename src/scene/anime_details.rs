use crate::list_entry::ListEntry;

pub struct AnimeDetails {
    anime: ListEntry,
    episode_duration: usize,
    start_date: usize,
    end_date: usize,
    average_score: usize,
    mean_score: usize,
}